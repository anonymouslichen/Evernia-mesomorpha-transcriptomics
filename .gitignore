# ── Raw sequencing data ──────────────────
*.fastq
*.fastq.gz
*.fq
*.fq.gz

# ── Assembly outputs (large) ─────────────
*.Trinity.fasta
trinity_full/
trinity_outputs/

# ── TransDecoder intermediates ───────────
longest_orfs/
*.pep
*.bed
*.gff3

# ── BLAST databases ──────────────────────
nr*
uniprot_*.fasta
*.ndb
*.nin
*.nsq
*.nhr
*.nto
*.ntf
*.pdb
*.pin
*.psq
*.phr

# ── BLAST outputs (large / intermediate) ─
blastp_out
blastp_seqids
blastp_seqids_unique
*.E.prunastri.*

# ── Salmon / abundance estimation ────────
salmon_index/
*.sf
quant.sf

# ── taxonomizr database ──────────────────
accessionTaxa.sql
names.dmp
nodes.dmp

# ── Gene count matrix (generated) ────────
Trinity.gene.counts.matrix

# ── Plot outputs (generated by R) ────────
*.pdf
*.png

# ── R session files ──────────────────────
.Rhistory
.RData
.Rprof.out
Rplots.pdf

# ── SLURM logs ───────────────────────────
slurm-*.out
slurm-*.err
*.log

# ── OS / IDE ─────────────────────────────
.DS_Store
Thumbs.db
.Rproj.user/
*.Rproj
